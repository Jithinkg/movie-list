const api1 = require('../utils/apilist/CONTENTLISTINGPAGE-PAGE3.json')

const movie3 = (state=api1.page, action) => 
{
    switch (action.type)
    {
        default:
        return state
    }
  
}

export default movie3;