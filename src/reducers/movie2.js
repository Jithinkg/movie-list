const api1 = require('../utils/apilist/CONTENTLISTINGPAGE-PAGE2.json')

const movie2 = (state=api1.page, action) => 
{
    switch (action.type)
    {
        default:
        return state
    }
  
}

export default movie2;